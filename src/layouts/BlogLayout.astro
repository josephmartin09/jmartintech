---
const { title, date, thumbnail} = Astro.props;
---

<div class="prose mx-auto p-6">
  <h1 class="text-4xl font-bold mb-2">{title}</h1>
  <p class="text-gray-500 text-sm mb-4">{date}</p>
  <img src={`/${thumbnail}`} alt="..." />
  <slot />
</div>
